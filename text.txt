<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>Neon Luck Casino</title>
  <style>
    * { box-sizing: border-box; }
    body {
      font-family: Arial, sans-serif;
      background: #0a001f;
      color: #fff;
      margin: 0;
      padding: 15px;
      min-height: 100vh;
    }
    h1 {
      text-align: center;
      color: #ff00ff;
      text-shadow: 0 0 15px #ff00ff;
      font-size: 1.8em;
      margin: 10px 0 5px;
    }
    .balance {
      text-align: center;
      font-size: 1.6em;
      margin: 10px 0 20px;
      color: #00ff88;
      font-weight: bold;
    }
    .tabs {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      justify-content: center;
      margin: 20px 0;
    }
    .tab {
      padding: 14px 18px;
      background: #1a0033;
      border: none;
      color: white;
      cursor: pointer;
      border-radius: 8px;
      font-size: 1.1em;
      flex: 1;
      min-width: 100px;
      text-align: center;
    }
    .tab.active {
      background: #ff00ff;
      box-shadow: 0 0 15px #ff00ff;
    }
    .game {
      display: none;
      background: #1a0033;
      padding: 20px;
      border-radius: 16px;
      max-width: 700px;
      margin: 0 auto;
    }
    .game.active { display: block; }

    button {
      padding: 16px 24px;
      margin: 8px 4px;
      background: #ff00ff;
      color: white;
      border: none;
      border-radius: 12px;
      cursor: pointer;
      font-size: 1.1em;
      font-weight: bold;
    }
    button:hover, button:active {
      background: #ff66ff;
      transform: scale(1.05);
    }
    input[type="number"] {
      padding: 14px;
      font-size: 1.1em;
      width: 140px;
      border-radius: 8px;
      border: none;
      margin: 8px 0;
    }

    /* Slots */
    .slot {
      font-size: 3.5em;
      display: inline-block;
      width: 90px;
      height: 90px;
      text-align: center;
      line-height: 90px;
      background: #000;
      margin: 8px;
      border-radius: 12px;
      box-shadow: 0 0 10px rgba(255,0,255,0.5);
    }
    #reels { text-align: center; margin: 20px 0; }

    /* Results */
    .result {
      margin-top: 20px;
      font-size: 1.4em;
      min-height: 60px;
      text-align: center;
      font-weight: bold;
    }

    @media (max-width: 480px) {
      .slot { width: 80px; height: 80px; font-size: 3em; line-height: 80px; }
      button { padding: 14px 20px; font-size: 1em; }
      h1 { font-size: 1.6em; }
    }
  </style>
</head>
<body>
  <h1>🌟 Neon Luck Casino 🌟</h1>
  <div class="balance">Balance: $<span id="balance">10000</span></div>

  <div class="tabs">
    <button class="tab active" onclick="switchTab(0)">🎰 Slots</button>
    <button class="tab" onclick="switchTab(1)">🃏 Blackjack</button>
    <button class="tab" onclick="switchTab(2)">🎡